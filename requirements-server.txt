# Server-specific requirements (FastAPI + Vosk)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=11.0.3
vosk>=0.3.45
python-dotenv>=1.0.0

# NumPy must stay < 2 for compatibility
numpy<2.0.0

# CPU-only PyTorch (no CUDA / nvidia deps)
torch==2.2.2+cpu
torchvision==0.17.2+cpu
torchaudio==2.2.2+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# Punctuation
deepmultilingualpunctuation==1.0.1
