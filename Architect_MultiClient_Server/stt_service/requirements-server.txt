# ============================================================================
# STT Service Requirements
# WebSocket-based Speech-to-Text service with Vosk and Whisper
# ============================================================================

# ========================================
# Web Framework & WebSocket
# ========================================
fastapi>=0.104.0               # FastAPI web framework for REST/WebSocket API
uvicorn[standard]>=0.24.0      # ASGI server with WebSocket support
websockets>=11.0.3             # WebSocket protocol implementation
pydantic>=2.0.0                # Data validation and settings management

# ========================================
# Speech-to-Text Engines
# ========================================
vosk>=0.3.45                   # Real-time Vosk STT engine
faster-whisper>=1.0.0          # Whisper transcription (post-processing)

# ========================================
# Database & Storage
# ========================================
motor>=3.3.0                   # Async MongoDB driver
pymongo>=4.5.0                 # MongoDB Python driver (sync operations)
minio>=7.2.0                   # MinIO client for audio file storage
redis>=5.0.0                   # Redis client with async support (redis.asyncio)

# ========================================
# HTTP Client
# ========================================
aiohttp>=3.9.0                 # Async HTTP client for external API calls

# ========================================
# Monitoring & Health Check
# ========================================
prometheus_client>=0.19.0      # Prometheus metrics collection
psutil>=5.9.0                  # System resource monitoring (CPU, memory)

# ========================================
# Configuration & Environment
# ========================================
python-dotenv>=1.0.0           # Load environment variables from .env
# Server-specific requirements (FastAPI + Vosk)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=11.0.3
vosk>=0.3.45
python-dotenv>=1.0.0
numpy>=1.24.0
PyJWT>=2.10.1
cryptography>=42.0.0

# Monitoring
prometheus_client>=0.19.0
psutil>=5.9.0

# ========================================
# Development & Quality
# ========================================
ruff>=0.4.0                    # Fast linter and code formatter
mypy>=1.10.0                   # Static type checker