# ============================================================================
# LiveKit Agent Requirements
# Real-time speech-to-text transcription + text-to-speech synthesis
# ============================================================================

# ========================================
# Core LiveKit & Real-time Communication
# ========================================
livekit-agents          # LiveKit agents framework for real-time applications>=0.5.0
livekit                # LiveKit client SDK for WebRTC communication=0.9.0 
websockets>=11.0.3             # WebSocket client for STT/TTS server communication
livekit-api                   #==0.5.2  

# ========================================
# Web Framework & API
# ========================================
fastapi>=0.104.0               # FastAPI web framework for dispatch API
uvicorn[standard]>=0.24.0      # ASGI server for FastAPI
pydantic>=2.0.0                # Data validation and settings management
httpx>=0.25.0                  # HTTP client for API calls
protobuf>=4.25.0               # Protocol buffers (for LiveKit)

# ========================================
# Audio Processing & Analysis
# ========================================
numpy>=1.24.0                  # Numerical computing for audio buffers
scipy>=1.11.0                  # Scientific computing for signal processing
librosa>=0.10.0                # Audio analysis and feature extraction
pyaudio>=0.2.13                # Audio I/O (VAD processor)

# ========================================
# Text-to-Speech (TTS)
# ========================================
torch>=2.0.0                   # PyTorch for Silero TTS model
torchaudio>=2.0.0              # Audio I/O for PyTorch
kokoro >=0.9.4

# ========================================
# Database & Storage
# ========================================
motor>=3.3.0                   # Async MongoDB driver
pymongo>=4.5.0                 # MongoDB Python driver (sync operations)

# ========================================
# Configuration & Environment
# ========================================
python-dotenv>=1.0.0           # Load environment variables from .env

# ========================================
# Authentication & Security
# ========================================
PyJWT>=2.10.1                  # JSON Web Token for authentication

# ========================================
# Notes
# ========================================
# - vosk: NOT REQUIRED in agent (server_vosk handles Vosk)
# - Audio flows: LiveKit → Agent → WebSocket → server_vosk (Vosk)
# - TTS: Agent downloads Silero model from https://models.silero.ai/
# - MongoDB: Optional (set ENABLE_MONGODB=false to disable)

