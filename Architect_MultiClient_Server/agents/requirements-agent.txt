# ============================================================================
# LiveKit Agent Requirements
# Real-time speech-to-text transcription + text-to-speech synthesis
# ============================================================================

# ========================================
# Core LiveKit & Real-time Communication
# ========================================
livekit-agents>=0.9.0          # LiveKit agents framework for real-time applications
livekit>=0.17.0                # LiveKit client SDK for WebRTC communication
livekit-api>=0.7.0             # LiveKit server API for room/token management
websockets>=11.0.0             # WebSocket client for STT/TTS server communication
protobuf>=5.0,<7.0              # Protocol buffers (for LiveKit). Ceiling required: googleapis-common-protos
                                # and opentelemetry-proto (both pulled in transitively, likely via LiveKit's
                                # own tracing) declare protobuf<7.0 -- an unbounded floor here lets pip resolve
                                # 7.x on a fresh install, breaking those two. See audio-ingestion/PLAN.md D31
                                # for the incident (recording_pb2.py was regenerated at gencode 6.33.5 to match
                                # this ceiling, not the other way around -- don't bump this without also
                                # re-running scripts/gen_proto.sh at a protobuf version still <7.0).

# ========================================
# API
# ========================================
pydantic>=2.0.0                # Data validation and settings management
httpx>=0.25.0                  # HTTP client for API calls

# ========================================
# Audio Processing & Analysis
# ========================================
numpy>=1.24.0,<2.0.0           # Numerical computing for audio buffers
scipy>=1.11.0                  # Scientific computing for signal processing
librosa>=0.10.0                # Audio analysis and feature extraction
pyaudio>=0.2.13                # Audio I/O (VAD processor)

# ========================================
# Database & Storage
# ========================================
motor>=3.3.0                   # Async MongoDB driver
pymongo>=4.5.0                 # MongoDB Python driver (sync operations)
boto3>=1.34.0                  # S3-compatible multipart upload (MinIO) -- old egress fallback, PLAN.md D2

# ========================================
# record-service forwarding (audio-ingestion/PLAN.md Phase 2)
# ========================================
grpcio>=1.62                   # gRPC client for forwarding raw PCM to record-service

# ========================================
# Configuration & Environment
# ========================================
python-dotenv>=1.0.0           # Load environment variables from .env

# ========================================
# Authentication & Security
# ========================================
PyJWT>=2.8.0                   # JSON Web Token for authentication

